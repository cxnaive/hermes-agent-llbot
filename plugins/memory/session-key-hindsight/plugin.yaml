name: session-key-hindsight
version: 1.0.0
description: "Hindsight scoped by gateway_session_key (per-chat). Subclasses bundled hindsight: inherits knowledge graph retain/recall/reflect + multi-strategy retrieval, only derives bank_id from the gateway session key."
pip_dependencies:
  - hindsight-client>=0.4.22
hooks:
  - on_session_end
